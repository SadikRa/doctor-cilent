import { useLoaderData } from "react-router-dom";

const UserBooking = () => {

    const data = useLoaderData()

    console.log(data)

    return (
        
        <div>
            
        </div>
    );
};

export default UserBooking;